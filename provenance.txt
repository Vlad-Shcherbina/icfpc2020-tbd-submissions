### Binary
cargo build -p julie --bin scarecrow_players --release


### Base commit
b26034c
join request fix
caudate_julie
Mon Jul 20 10:21:39 2020 +0300


### Local changes on top of this base commit

diff --git a/scratches/julie/src/bin/predicting_player.rs b/scratches/julie/src/bin/predicting_player.rs
index 8771b3b..2c71897 100644
--- a/scratches/julie/src/bin/predicting_player.rs
+++ b/scratches/julie/src/bin/predicting_player.rs
@@ -1,10 +1,10 @@
 use tbd::runners::{run_in_submission, run_bots_local};
 use julie::predicting_ai::PredictingAi;
+use tbd::uforest::{JoinRequest, ShipParams, GameSpec, Data, GameState, Commands };
 
 fn main() {
     if tbd::is_running_in_submission() {
-        let upgrades = Data::make_list2( 103652820, 192496425430);
-        JoinRequest { upgrades }
+        run_in_submission(PredictingAi {});
     } else {
         run_bots_local(PredictingAi {}, PredictingAi {});
     }
diff --git a/scratches/julie/src/bin/scarecrow_players.rs b/scratches/julie/src/bin/scarecrow_players.rs
index 38d872e..a4d1596 100644
--- a/scratches/julie/src/bin/scarecrow_players.rs
+++ b/scratches/julie/src/bin/scarecrow_players.rs
@@ -1,6 +1,7 @@
 use tbd::runners::{run_in_submission, run_bots_local};
 use tbd::ai_interface::Ai;
-use tbd::uforest::{JoinRequest, ShipParams, GameSpec, Data, GameState, Commands };
+use tbd::bot_util::{ships_by_role };
+use tbd::uforest::{JoinRequest, ShipParams, GameSpec, Data, GameState, Commands, Command };
 
 // fuel x1, laser x4, cooling x12, hull x2
 
@@ -72,16 +73,17 @@ impl Ai for ScarecrowBot {
     }
 
     fn choose_commands(&mut self, spec: &GameSpec, _state: &GameState) -> Commands {
-        Commands(vec![])
+        let me = ships_by_role(_state, spec.role).next().unwrap();
+        Commands(vec![Command::Detonate{ ship_id: me.ship_state.ship_id}])
     }
 }
 
 
 fn main() {
     if tbd::is_running_in_submission() {
-        run_in_submission(ScarecrowBot {t: ScarecrowType::Sniper});
-        run_in_submission(ScarecrowBot {t: ScarecrowType::Carrier});
-        run_in_submission(ScarecrowBot {t: ScarecrowType::Kamikaze});
+        // run_in_submission(ScarecrowBot {t: ScarecrowType::Sniper});
+        // run_in_submission(ScarecrowBot {t: ScarecrowType::Carrier});
+        // run_in_submission(ScarecrowBot {t: ScarecrowType::Kamikaze});
         run_in_submission(ScarecrowBot {t: ScarecrowType::KamikazeCrew});
     } else {
         run_bots_local(
